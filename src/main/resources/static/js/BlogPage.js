/**
 * BLOGPAGE.JS - TỐI ƯU
 * Logic cho trang danh sách blog
 */

// Chỉ load khi ở trang Blog List
if (document.body.classList.contains('blog-page') || location.pathname.includes('/blogs')) {
    document.addEventListener('DOMContentLoaded', function() {
        console.log('=== BLOG PAGE LOADED ===');

        // Pagination, filtering logic
    });
}
