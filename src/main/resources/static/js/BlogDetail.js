/**
 * BLOGDETAIL.JS - TỐI ƯU
 * Logic cho trang chi tiết blog
 */

// Chỉ load khi ở trang Blog Detail
if (document.body.classList.contains('blog-detail-page') || location.pathname.includes('/blog/')) {
    document.addEventListener('DOMContentLoaded', function() {
        console.log('=== BLOG DETAIL PAGE LOADED ===');

        // Share buttons, comments, etc.
    });
}
